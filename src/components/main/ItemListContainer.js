import "./ItemListContainer.scss";

const ItemListContainer = (props) => {
  return (
    <div>
      <div class="greeting">{props.greeting}</div>
    </div>
  );
};

export default ItemListContainer;
